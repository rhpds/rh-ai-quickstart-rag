---
# Ansible Galaxy Requirements
# These collections will be installed before playbook execution

collections:
  # Core Kubernetes support - for k8s module
  - name: kubernetes.core
    version: ">=2.3.0,<4.0.0"

  # General community modules
  - name: community.general
    version: ">=5.0.0,<10.0.0"

  # NOTE: agnosticd.showroom and agnosticd.core are included locally
  # in the collections/ directory to avoid external dependency issues.
  # TODO: Update to use upstream collections when agnosticd.core is
  # properly packaged and published.
